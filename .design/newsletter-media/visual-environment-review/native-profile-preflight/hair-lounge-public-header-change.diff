diff --git a/app/hairlounge-chrome.css b/app/hairlounge-chrome.css
index d4580eb..1f938e1 100644
--- a/app/hairlounge-chrome.css
+++ b/app/hairlounge-chrome.css
@@ -28,6 +28,7 @@
   max-width: 1240px;
   margin: 0 auto;
   padding: 0 clamp(20px, 4vw, 40px);
+  min-width: 0;
   display: flex;
   align-items: center;
   justify-content: space-between;
@@ -38,12 +39,16 @@
   display: block;
   text-decoration: none;
   flex-shrink: 0;
+  min-width: 0;
 }
 
 .hl-wordmark {
   display: block;
   font-family: var(--hl-serif);
-  font-size: 30px;
+  /* Scales with the viewport so the row fits a 320–390px phone: at 30px the
+     letter-spaced wordmark plus "Book Online" and the burger needed ~485px and
+     the burger sat off-screen, unreachable (customer test 13 Sep 2026). */
+  font-size: clamp(22px, 6.5vw, 30px);
   letter-spacing: 0.16em;
   color: var(--hl-cream);
   line-height: 1;
@@ -192,6 +197,24 @@
   letter-spacing: 0.06em;
 }
 
+.hl-mobilenav-book {
+  color: var(--hl-gold-bright);
+}
+
+/* On phones the header keeps only the wordmark and the burger; the booking
+   call-to-action moves into the menu (and the hero already carries one). */
+@media (max-width: 639px) {
+  .hl-bookbtn {
+    display: none;
+  }
+}
+
+@media (min-width: 640px) {
+  .hl-mobilenav-book {
+    display: none;
+  }
+}
+
 @media (min-width: 1024px) {
   .hl-nav {
     display: flex;
diff --git a/components/hairlounge/header.client.tsx b/components/hairlounge/header.client.tsx
index 46f42d4..de73124 100644
--- a/components/hairlounge/header.client.tsx
+++ b/components/hairlounge/header.client.tsx
@@ -148,6 +148,21 @@ export function HlHeaderClient({
         <a href={phoneHref} className="hl-mobilenav-tel" onClick={() => setOpen(false)}>
           {phone}
         </a>
+        {bookHref.startsWith('http') ? (
+          <a
+            href={bookHref}
+            target="_blank"
+            rel="noopener noreferrer"
+            className="hl-mobilenav-book"
+            onClick={() => setOpen(false)}
+          >
+            {bookLabel}
+          </a>
+        ) : (
+          <Link href={bookHref} className="hl-mobilenav-book" onClick={() => setOpen(false)}>
+            {bookLabel}
+          </Link>
+        )}
       </div>
     </header>
   );
