# Newsletter media implementation evidence

Updated: 2026-09-19. **11/51 sites have completed release and post-deploy verification:** the four pilots plus Academy Partnership, Bakewell Pudding, Bank House, Bath Street Dental, Blue Anchor, Bodalwyn and Bubbleton. Popty and ASAP also passed actual storage round trips. The serial queue has reached Burford Road Dental; 40 sites remain. Rollout is incomplete. See [SESSION_STATUS.md](./SESSION_STATUS.md) and [ROLLOUT.json](./ROLLOUT.json).

## Implemented and verified

- Shared Plate image controls, serializer, upload/library picker and attachments preserve the existing Puck string picker contract. Images support alt/decorative state, caption/link, width/alignment, replacement/removal and undo/redo. Changed code modules stay below 600 lines.
- Saved `bodySource` explicitly selects visual, HTML or text content. Switching views preserves the visual document; resuming it or replacing it from text requires confirmation. Arbitrary HTML-to-Plate conversion is outside scope.
- The public walkthrough shares the controls and serializer, with session-only files, PNG/PDF fixtures and simulated sending. Newsletter browser tests cover this simulation; installed-site storage verification is a separate pilot step.
- Trusted upload receipts and reviewed migration provenance establish site ownership. Unknown public file keys cannot fall through to the shared bucket or create Asset records. Durable image renditions survive source deletion and use the existing `{{appUrl}}` token.
- Attachment preparation verifies actual ownership, bytes, MIME/signature and encoded message size. Private snapshots use AES-256-GCM with a dedicated per-site key. Durable pending records precede storage writes; interrupted writes resume, and only ready records can load. R2 reads have a 60-second response/body deadline.
- Queuing freezes content and media before publication. Bytes and footer icons are prepared once per batch. Atomic delivery claims prevent concurrent submissions; ambiguous provider outcomes require review without automatic retries. Interrupted queue preparation requires operator recovery.
- Private snapshots remain pinned while referenced, then become eligible for bounded cleanup at least 30 days after release. Sent image renditions remain durable. Cleanup has a dry-run command and a daily scheduler vehicle.
- Social links use per-site HTTPS settings and omit unset entries. Provisioning preserves ASAP's five configured social accounts. Installed-demo real newsletter sending is rejected by the new server code unless explicitly opted in; provisioning alone does not enforce that policy in older live slots.
- Selected newsletter image assets and attachments are frozen. Third-party custom HTML/news-highlight image URLs and standard branding retain their previous behavior; arbitrary remote images are not fetched or frozen.

## Completed evidence

| Area | Verified result |
| --- | --- |
| Hub release | Exact commit `3ac883e520438f9f1684b247c3b57342f4eeb3c4` passed every required gate, deployed to blue port 3061 and was pushed to canonical GitHub. |
| Hub gates | Lint, typecheck, 142 unit tests, 13 isolated Mongo tests, 17 release-runner tests, build, portfolio check, 52 browser tests, required Lighthouse gates and theme smoke passed. Four existing conditional browser skips remain; no baselines or thresholds were relaxed. |
| Lighthouse evidence | Hub: all four required category medians are 100 for `/`, `/about` and `/audit` on mobile/desktop; all 18 individual reports also score 100. Popty: required gates passed and all category medians are 100 for `/` and `/about`; chronological home performance runs are mobile 96/100/100 and desktop 100/75/100. Every other Popty category/run scores 100. Existing LHCI assertions use the default optimistic aggregation; medians were independently calculated from the raw reports. |
| Installed-demo release | Popty Cara commit `9fd8e3702c5758e6c735495476ff2ec0c552e120` completed healthy at 19:13:25 UTC after its required gates. Actual R2 image and encrypted-attachment round trips, exact public image bytes, private-route denial and deployed demo-send rejection passed. Read-only cron and daily cleanup are installed. Exactly two temporary media records remain for ordinary cleanup; no emails, campaigns, recipients or source assets were written. The existing demo proxy intentionally overrides public image cache headers with no-store; immutable identity and checksum ETag were verified. |
| Current rollout runner | Root HEAD `89e01babef583581ca11ea7884c34a8f5a9cf37f` includes fresh three-run Lighthouse median checks, reviewed font environments and safe generated state/cache handling. Its 37 tests/171 assertions, scoped lint and full typecheck pass. These operations follow-ups are distinct from the 17 runner tests included in deployed hub commit `3ac883e`. Existing Lighthouse configuration is unchanged. |
| ASAP release and scheduler | Exact `de6daf044b5335a4fa74c529a82637a2c5c47708` passed all site/admin gates and Lighthouse medians, is healthy on blue3103 and is mirrored to GitHub. Stable-origin dispatch and daily cleanup replaced only the broken newsletter cron. All six queue counts stayed zero, historical sent deliveries stayed three, and all five intended footer social settings match source/clone/live. The first natural tick at 19:55:01.652 UTC succeeded with zero campaigns/sends/failures/review; no manual dispatch was invoked. |
| ASAP storage and recovery | Actual separate-bucket image/encrypted-attachment round trips, immutable public headers and private-route denial passed; complete original Asset document/object fingerprints are unchanged. Exactly two temporary media records remain for normal cleanup. The deploy script's untracked metadata stopped the runner after successful deployment; the narrow state fix passed 28 tests/122 assertions, lint, typecheck and independent review. Completed gates, unchanged candidate/environment and live bytes were reverified before finishing controller promotion and mirroring, without rebuilding or switching slots. Original failure/rollback evidence is retained. |
| The Brave release and operations | Exact `3762efcaeb4ff673faeebe20b2b9171a307306c2` passed all required gates and Lighthouse medians, is healthy on green3017, and has completed controller promotion and GitHub mirroring. Tracked clone deployment state was captured/restored while original dirty state remained intact. Nginx media isolation, authenticated empty-queue checks, stable-origin dispatch and daily cleanup passed; no verification emails were sent. |
| Concurrency and recovery | Isolated Mongo tests cover overlapping dispatch, immutable queue versions, rescheduling after candidate selection, cancellation, provider ambiguity, acknowledgement failure, worker death and read-only cron behavior. Provider calls are mocked. |
| Hub operations | Exact live source hashes and public health verified. Nginx media routing applied; cross-site keys return 404 without backfill, unknown renditions return 404 and an existing legacy profile still returns 200. Empty queue verified before/after five-minute future dispatch and daily cleanup installation; zero emails sent during verification. |
| Ownership migration | All 1,918 reviewed records applied and independently checked: 1,359 installed-demo records, 528 ASAP and 31 The Brave. Eight excluded records remain unchanged and untrusted. Four active exclusions require reupload before newsletter use; four are trashed ASAP records. |
| Configuration | All 51 managed sites have consistent controller/slot media encryption keys and cron secrets, with private backups. The Brave's separate release controller received its two missing newsletter keys. Provisioning did not restart services. |
| Shared source | All 98 reviewed shared files match feature source `ffc397d03d0cc3759ca7375515953a576bd096a4` and template `87c7e45f580325abdee3cb5abb5eb51899df1692`. The explicit sync allowlist, future installer provisioning and nginx template are updated. |
| Fleet staging | The complete original fleet is staged in isolated clones, including all 40 sites still awaiting release. Complete Asset bookkeeping prerequisites are included. Site-specific changes, ASAP's 200-recipient cron batch setting and unrelated dirty files are preserved. Staging is not deployment. |
| First four further demos | Academy Partnership `02ac1730cf876a289d97761f23213891f9c1092b`, Bakewell Pudding `f9d90e4a5b255fe3e1dd875886acefe2be6130f5`, Bank House `6184659a1b476863b93b98cc8a6ece1cc06ccbd3` and Bath Street Dental `84c702cc75d2fb3714a597dc1661b61881411f9f` each passed complete gates, deployed healthy, promoted their controller and passed separate nginx/media/read-only scheduler/daily cleanup checks. These four were the first additional completions after the pilots. |
| Resolved Academy first-attempt history | Candidate `02ac1730cf876a289d97761f23213891f9c1092b` initially failed public comparisons: desktop home 1,351 pixels, mobile home 414 and mobile about 148; 11 browser tests passed and two were skipped. That attempt stopped before deployment. Confirmed host font drift was corrected with a reviewed quality-only profile restoring all four original screenshots; the same candidate subsequently passed its complete gates and release. No baseline, threshold or production font changed. |
| Resolved Blue Anchor retry | Its earlier admin visual port conflict stopped deployment. With the port free, every gate passed on a fresh run and exact `b966cf2aea31633f3eac879be2e9f0d77f170a79` deployed healthy, followed by successful operations checks. Bodalwyn `58547a68b707486a3219ba054f06dbad1633e683` subsequently completed too. Original failures remain under `fleet-release-history/`. |
| Resolved Bubbleton layout shift | The independent median guard rejected desktop home scores 100/99/99. The site-only static-cache correction `17b69dd` preserves Next immutable font/CSS caching while retaining page/API no-store and demo restrictions. Final `48dae05875a01362f41282fb51055b9a917649d0` passed every gate with unchanged screenshots and all 12 Lighthouse reports at 100 in required categories, then deployed healthy and passed operations. Public page no-store and stylesheet immutable caching were verified. Evidence: `fleet-release-history/2026-09-19-bubbleton-cls/`, `fleet-release-final/bubbleton-tenby*.json`. |
| Owner instructions | [Newsletter images and attachments](../../docs/newsletter-media.md) documents the shipped controls, exact attachment limits, explicit source conversion, queued media copies, demo behavior and delivery review. |

Release evidence: [hub post-deploy report](./hub-release/post-deploy.json), [previous hub slot](./hub-release/previous-live.json), [hub Lighthouse scores](./hub-release/lighthouse-summary.json), [Popty Lighthouse scores](./pilot-release-final/popty-cara-lighthouse-summary.json), [Popty release](./pilot-release-final/popty-cara.json), `/tmp/newsletter-hub-release-reviewed.log`, [template receipt](./template-distribution-result.json), [pilot staging](./PILOT_STAGING.md), [fleet staging](./fleet-staging-final/verification.json), [ownership review](./ownership-review/) and [demo operations](./demo-operations/README.md).

Additional completed pilots: [ASAP release](./pilot-release-final/asap.json), [ASAP post-deploy and natural cron tick](./pilot-release-final/asap-post-deploy.json), [ASAP storage smoke](./pilot-release-final/asap-live-storage.json), [The Brave release](./pilot-release-final/thebrave.json), [The Brave operations](./pilot-release-final/thebrave-post-deploy.json) and [The Brave Lighthouse scores](./pilot-release-final/thebrave-lighthouse-summary.json).

Fleet completion evidence: [Academy release](./fleet-release-final/academy-partnership.json) and [operations](./fleet-release-final/academy-partnership-post-deploy.json); [Bakewell release](./fleet-release-final/bakewell-pudding.json) and [operations](./fleet-release-final/bakewell-pudding-post-deploy.json); [Bank House release](./fleet-release-final/bank-house.json) and [operations](./fleet-release-final/bank-house-post-deploy.json); [Bath Street Dental release](./fleet-release-final/bath-street-dental.json) and [operations](./fleet-release-final/bath-street-dental-post-deploy.json). The [archived Blue Anchor failure](./fleet-release-history/2026-09-18-blue-anchor-port/blue-anchor.json), [log](./fleet-release-history/2026-09-18-blue-anchor-port/blue-anchor.log), [old queue summary](./fleet-release-history/2026-09-18-blue-anchor-port/summary.json) and [old post-deploy status](./fleet-release-history/2026-09-18-blue-anchor-port/post-deploy-status.json) preserve the failed attempt. Follow the [fresh Blue Anchor report](./fleet-release-final/blue-anchor.json) for the retry.

The inventory covers 51 managed sites: the hub, ASAP, The Brave and 48 installed demos. The separate `ots-sauro-poc` slot-only exception is outside that managed fleet. The Brave release base is `c04ccd5`, preserving its later owner-workflow/editor fixes; do not release the older detached `aa8cac5` checkout.

## Operational requirements

- Before any cron POST, prove the deployed endpoint is the new read-only-capable implementation. Old endpoints can ignore a `dryRun` request. `bun scripts/newsletter-dispatch-cron.ts --env-file=<production-env>` then performs the authenticated read-only check.
- Install schedulers serially with `bun scripts/newsletter-scheduler.ts --site-root=<controller> --install`. The default installs read-only checks and daily cleanup. `--enable-dispatch` additionally requires a verified empty queue and explicit demo opt-in where applicable. Backups are private; execution uses the stable HTTPS origin, timeouts and `flock`.
- Each site's release record must separately show live commit/health, nginx media routing, dispatch mode and daily cleanup installation. The fleet release runner does not perform the latter operations. Demos require cleanup even while real sending is disabled.
- `bun scripts/newsletter-media-cleanup.ts --env-file=<production-env>` is read-only unless `--apply` is supplied. Keep the media key while retained snapshots need it. Sent image storage must survive code rollback.
- Old slots lack frozen-manifest handling and the immutable-media endpoint. After feature usage, do not perform a generic old-slot rollback: first block newsletter admin mutation/dispatch endpoints and disable scheduled dispatch; inspect queued, sending and uncertain campaigns. Preserve the new image route/service and durable storage, or deploy a corrected media-capable commit. Record the concrete dispatch block before attempting rollback. Captured previous slots are recovery evidence, not authorization for blind rollback.
- Legacy migration uses reviewed manifests and checksum/CAS checks; a database record alone is insufficient ownership proof. The eight exclusions must not be automatically trusted.
- Fleet release uses the reviewed runner and each site's complete quality manifest, then the exact validated commit in an inactive slot. See [release instructions](../../ops/scripts/NEWSLETTER_RELEASE.md). Browser checks use local Playwright; Figma and Snapshot MCP are prohibited for this task. No real emails are a verification mechanism.

## Remaining before completion

Academy's first release attempt stopped before deployment on three public screenshot comparisons. Its unchanged live version reproduced exactly the same differences. Microsoft core fonts and OpenSymbol installed on 11 September changed rendering since the 4 September baselines; a process-only profile excluding those additions restores all four original screenshots. The release runner now checks an explicit environment catalog against each exact baseline tree, records profile/font hashes and confines the setting to quality subprocesses. The change passed independent review, 31 runner tests/132 assertions, lint and typecheck. No baseline, threshold or production font was changed. Evidence: [font diagnosis](./visual-environment-review/academy-fontconfig-diagnosis.json), [source comparison](./visual-environment-review/academy-font-analysis.json) and [environment catalog](./visual-environments.json). A retry also exposed the tracked TypeScript build cache used by46demos. The runner now archives valid generated bytes and restores only the originally clean clone cache around successful or failed quality runs. Pre-existing edits and unexpected data remain preserved. Real compiler regression tests and the combined37 runner tests/171 assertions, lint/typecheck and independent review pass. Complete fleet gates still remain required.

1. Monitor queue `9557` and post-deploy watcher `60107`; the release queue has reached Burford Road Dental. Earlier failures are archived and resolved with fresh gates. No baseline or threshold changes.
2. Complete all 40 remaining demos serially and verify each site's new send restriction, media route and cleanup schedule. Run separate serial post-deploy work only after healthy release evidence. Record exact commits, gates, health, previous slots and canonical-remote mirror skips.
3. Keep tasks 8 and 9 open until fleet validation and release/operations are complete. Eleven sites are complete; successful pilots and early demos, completed migrations and staged source do not establish live enforcement on the remaining sites.
